const shapes = (data) => {
    class shapeCon {
    constructor(text, textColor, shape, color){
        this.text = text;
        this.textColor = textColor;
        this.shape = shape;
        this.color = color;
    }
}
}
module.exports=shapes;