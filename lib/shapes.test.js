const shapes = require('./shapes')

describe('shapes', ()=>{
    describe('triangle', ()=>{
        it('should take in a color and add it to the background'), ()=>
       { const color = blue;
        const triangle = new Triangle();
        expect(triangle.render()).toEqual(`<circle cx='150' cy='110' r='60' stroke='transparent' fill='blue' stroke-width='5'/>`);
    }
    })
})
